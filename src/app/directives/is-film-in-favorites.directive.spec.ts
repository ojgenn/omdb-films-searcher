import { IsFilmInFavoritesDirective } from './is-film-in-favorites.directive';

describe('IsFilmInFavoritesDirective', () => {
  it('should create an instance', () => {
    const directive = new IsFilmInFavoritesDirective();
    expect(directive).toBeTruthy();
  });
});
